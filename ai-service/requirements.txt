# Python dependencies — install with: pip install -r requirements.txt
# NOTE: OCR itself runs via MinerU, in a separate isolated venv
# (ai-service/mineru_venv/, see mineru_ocr_service.py) — not installed from
# this file. See SETUP.md for how to set that venv up.

fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
requests==2.34.2
httpx==0.28.1

# Database
psycopg2-binary==2.9.12

# Lightweight dictionary-based spellcheck (no model inference)
pyspellchecker

# Local ML inference (question generation, zero-shot classification)
# Loose pins so pip can resolve a compatible build for the active Python version.
torch>=2.3.0
transformers>=4.41.1
sentencepiece
